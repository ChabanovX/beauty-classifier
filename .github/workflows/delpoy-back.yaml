name: Deploy to VM

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ml-vm-runner
    
    steps:
    - name: Pull code
      run: |
          git pull
    - name: Stop and restart containers
      run: docker compose down && docker compose build && docker compose up --remove-orphans
